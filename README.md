# Emby Tool

### Usage

Create file `auth.json` and add API token and server URL.

``` json
{
  "ApiToken": "<token>",
  "ServerId": "<server-id-copied-from-emby-item-url>",
  "ServerUrl": "<url>"
}
```

Then use these scripts.
